2026-05-01: Implemented `yt playlists create` (Milestone 1, item 1a).
  - New file: internal/cmd/playlists_create.go
  - Wired into playlists.go subcommand list
  - Flags: --title (required), --description, --privacy {private|unlisted|public}, --dry-run
  - Validates privacy values; --dry-run prints planned mutation + 50-unit quota cost without calling API
  - Renders created playlist via output.Render (table/json/plain)
  - Verified: go vet passes, build clean, --help/--dry-run/invalid-privacy/missing-title all behave correctly

2026-05-01: Implemented `yt playlists update <id>` (Milestone 1, item 1b).
  - New file: internal/cmd/playlists_update.go
  - Wired into playlists.go subcommand list
  - Flags: --title, --description, --privacy {private|unlisted|public}, --dry-run
  - Patch semantics: fetches current snippet+status (1 unit), overlays only the flags the user passed via cmd.Flags().Changed, then sends a single playlists.update (50 units)
  - Requires at least one of --title/--description/--privacy; validates privacy enum; refuses empty resulting title
  - --dry-run prints planned merged values + cost without calling update
  - Renders updated playlist via output.Render (table/json/plain)
  - Verified: go vet passes, build clean, --help shows quota cost, missing-flags and invalid-privacy errors fire before any API call

2026-05-01: Implemented `yt playlists delete <id>` (Milestone 1, item 1c) — completes Milestone 1 step 1.
  - New file: internal/cmd/playlists_delete.go
  - Wired into playlists.go subcommand list
  - Flags: --yes (skip confirmation), --dry-run
  - Fetches current snippet (1 unit) so the confirmation prompt can show the playlist title and a missing id errors out before any 50-unit call
  - Default flow prompts on stderr, reads from cmd.InOrStdin(), requires literal "yes" to proceed (any other input aborts cleanly)
  - --yes bypasses the prompt; --dry-run prints the planned deletion + cost without calling delete
  - On success prints "Deleted playlist id=… title=…" to stdout
  - Verified: go vet passes, build clean, `playlists delete --help` shows quota cost & flags, arg validation fires before any API call, command appears in `playlists` subcommand list

2026-05-01: Implemented `yt items add <playlist-id> <video-id-or-url>...` (Milestone 1, item 2a).
  - New file: internal/cmd/items_add.go
  - Wired into items.go subcommand list
  - Args: <playlist-id> followed by one or more video references; cobra.MinimumNArgs(2) enforces both
  - Accepts raw 11-char ids OR YouTube URLs (watch?v=, youtu.be/<id>, /shorts/<id>, /embed/<id>, /v/<id>) — resolves Open Question #1 affirmatively
  - parseVideoID() lives in the same file; rejects empty values and URLs from which no id can be extracted, before any API call
  - Inserts each video sequentially via playlistItems.insert (snippet only); first failure aborts and surfaces the offending videoId in the error
  - --dry-run prints "would add N video(s) to playlist X (cost: 50N units)" + a PLAYLIST_ID/VIDEO_ID table of resolved ids without calling the API
  - On success renders POS / ITEM_ID / VIDEO_ID / TITLE for each created item, JSON output is the raw []PlaylistItem
  - Verified: go vet passes, build clean, `items add --help` documents 50-unit cost, arg-count + URL-parse errors fire pre-API, dry-run with 4 mixed inputs (raw id, watch?v, youtu.be, /shorts) correctly resolves all four

2026-05-01: Implemented `yt items remove <item-id>...` (Milestone 1, item 2b).
  - New file: internal/cmd/items_remove.go
  - Wired into items.go subcommand list
  - Args: one or more playlistItemIds; cobra.MinimumNArgs(1) enforces presence
  - Takes playlistItemId (ITEM_ID column from `items list`), NOT videoId — long help spells this out because the same video can appear multiple times in a playlist with distinct item ids, so the API requires the item id to disambiguate. Skipped the "resolution helper" mentioned in the PRD note: scoped to a single task; `items list` already exposes ITEM_ID, and a videoId→itemId lookup can land later if there's real demand.
  - Flags: --yes (skip confirmation), --dry-run
  - Default flow prompts on stderr ("About to remove N item(s)... Type 'yes' to confirm: "), reads from cmd.InOrStdin(); any non-"yes" input aborts cleanly before any API call
  - --dry-run prints "would remove N item(s) (cost: 50N units)" + the list of item ids on stdout, no API call
  - Trims whitespace from each arg and rejects empty values pre-API
  - Sequential playlistItems.delete calls; first failure aborts and surfaces the offending itemId. On success prints "Removed item id=…" per deletion
  - Verified: go vet passes, build clean, `items remove --help` documents 50-unit cost and the playlistItemId requirement, missing-args errors fire pre-API, dry-run with 3 ids prints the correct cost (150 units), confirmation abort on "no" returns exit 1 without calling the API
